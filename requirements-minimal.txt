# Minimal requirements for Watchtower (Windows fallback)
# Use this if the main requirements.txt fails to install

# Core API and web framework
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# Database
duckdb

# Basic data handling (no heavy dependencies)
python-dotenv
loguru

# Optional: Add these if they install successfully
# streamlit
# pandas
# numpy
# plotly
